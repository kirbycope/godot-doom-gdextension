# Godot 4+ specific ignores
.godot/
/android/
.DS_Store
test-results/
.mcp/
extension_api.json

# The GDExtension's own build output is ignored by addons/pure_doom/.gitignore, which
# travels with the addon so it works the same wherever the addon is dropped.
